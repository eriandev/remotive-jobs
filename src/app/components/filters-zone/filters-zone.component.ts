import { Component } from '@angular/core';

@Component({
  selector: 'app-filters-zone',
  templateUrl: './filters-zone.component.html',
  styleUrls: ['./filters-zone.component.css'],
})
export class FiltersZoneComponent {
  public currentFilter?: string;
}
