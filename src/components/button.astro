---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'button'> {
  outline?: boolean
}

const { class: extraClass, outline = false, ...restProps } = Astro.props
---

<button {...restProps} class:list={['btn', outline ? 'outline' : 'fill', extraClass]}>
  <slot />
</button>
